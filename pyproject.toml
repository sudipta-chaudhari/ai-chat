[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openai-chat"
version = "0.1.0"
description = "OpenAI Chat project"
dependencies = [
    "openai>=1.3.0"
]
